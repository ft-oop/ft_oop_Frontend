export const messageHandler = (message, type) => {
  // 메시지 타입에 따라 분기 처리
  // 친구의 상태 (온라인, 오프라인)
  // 방 목록 (이름, 상태(게임중인지, 인원이 다 찼는지), 인원, 모드(1:1, 토너먼트), 비밀번호 여부)
  // 채팅 메시지 (보낸 사람, 받는 사람 또는 방, 내용, 시간)
  // 게임 (위치정보(공, 바), 점수, 준비 상태, 스킬, 게임 결과)
  // 1:1 랜덤 매칭 풀 소켓으로 할지 말지?? 모름
};
